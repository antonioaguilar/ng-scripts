describe('test @@COMPONENT_NAME service', function () {
  var @@COMPONENT_NAME, $rootScope;

  beforeEach(module('app'));
  beforeEach(module('services.@@COMPONENT_NAME'));

  beforeEach(inject(function (_$rootScope_, _@@COMPONENT_NAME_) {
    $rootScope = _$rootScope_;
    @@COMPONENT_NAME = _@@COMPONENT_NAME_;
  }));

  it('should pass', function () {
    expect(true).toBe(true);
  });

});
