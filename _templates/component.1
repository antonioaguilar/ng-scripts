${notice}
angular.module('components.${name}', [])
  .component('${name}', {
    bindings: {},
    controller: ${name},
    templateUrl: 'components/${style}/${style}.tpl.html'
  });

function ${name}() {

  this.$onInit = function() {};

  this.$onChanges = function(changes) {};

  this.$postLink = function() {};

  this.$onDestroy = function() {};

}
