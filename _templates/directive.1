${notice}
angular.module('directives.${name}', []).directive('${name}', ${name});

function ${name}() {

  function link(scope, element, attr) {

  }

  return {
    templateUrl: 'directives/${style}/${style}.tpl.html',
    link: link
  };

}
